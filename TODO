* Verify that config (--config) is being set in smrtsv.py for each option retrieved by get_config_param() in the comonent's snakefile.

* Strip @RG and @PG tags from assemble/local_assemblies.bam
  * Excessive, breaks other tools

# HG02106
$ samtools view -H assemble/local_assemblies.bam | cut -f1 | sort | uniq -c
      1 @HD
 670368 @PG
 502776 @RG
    195 @SQ


